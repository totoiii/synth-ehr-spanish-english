# Core ML dependencies
# NOTE: Versions managed by Unsloth installation - DO NOT specify versions here
# torch (installed by unsloth[cu118-torch260])
# transformers (installed by unsloth_zoo)  
# xformers (installed by unsloth)
datasets>=2.14.0
huggingface-hub>=0.19.0
accelerate>=0.34.0
peft>=0.13.0
bitsandbytes>=0.44.0
# trl (installed by unsloth_zoo)

# Unsloth - Fast finetuning (already installed in Dockerfile)
# unsloth

# Training utilities
tqdm>=4.66.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Logging and monitoring
tensorboard>=2.14.0
wandb>=0.15.0

# General utilities
python-dotenv>=1.0.0
colorlog>=6.7.0
pyyaml>=6.0
colorlog>=6.7.0
requests>=2.31.0

# Inference acceleration (optional)
# NOTE: may require a CUDA-compatible build; if install fails, set --use-vllm False
vllm
